/* Split point for Framer Motion's animation features.
   Imported dynamically by MotionProvider so the ~25kb of DOM animation code
   downloads after first paint instead of blocking it. */
export { domAnimation as default } from 'framer-motion'
